import Layout from "../src/shared/components/Layout";
import RegisterForm from "../src/user/components/RegisterForm";

const RegisterPage = () => {
  return (
    <Layout>
      <RegisterForm />
    </Layout>
  );
};

export default RegisterPage;
